Cognigy Cheat Sheet
This version separates the embedded Base64 images from `index.html`.
Embedded image references replaced: 19
Unique image files created: 18
Images are stored in: `images/`
Upload `index.html` and the complete `images` folder to the root of the GitHub repository.
